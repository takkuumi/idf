# 固件可靠性全面审计报告
**日期**: 2026-09-20  
**版本**: 2.2.4  
**审计范围**: 内存安全、并发安全、协议兼容、掉电安全、网络稳定性、IO可靠性、LoRa功能缺失

---

## 一、内存安全检查

### 1.1 栈使用审计

| 任务 | 配置栈 | 实际使用 | 余量 | 状态 |
|------|--------|----------|------|------|
| main_loop | 32KB | ~28KB | 4KB | ⚠️ 偏紧 |
| DeviceActor | 16KB | ~4KB | 12KB | ✅ 健康 |
| mb-rtu-master | 8KB | ~3KB | 5KB | ✅ 健康 |
| mb-rtu-slave | 8KB | ~2.5KB | 5.5KB | ✅ 健康 |
| udp-mcast | 6KB | ~2KB | 4KB | ✅ 健康 |
| nfc-st25 | 8KB | ~3KB | 5KB | ✅ 健康 |
| http-srv | 12KB | ~6KB | 6KB | ✅ 健康 |

**问题**:
- ❌ **CRITICAL**: `main_loop` 栈余量仅4KB，BLE回调深度增加可能导致Stack canary

**修复**:
- [ ] 将BLE GATT业务逻辑进一步剥离到`ble_cmd_queue`
- [ ] 增加栈健康监控阈值到2KB（当前1KB）

### 1.2 PSRAM分配审计

**已正确使用PSRAM的大对象**:
- ✅ `StorageSnapshot.holding_buf` (4096B)
- ✅ `StorageSnapshot.proto` (3000B)
- ✅ `StorageSnapshot.device_text` (4000B)
- ✅ NFC工作区 (4KB)
- ✅ BLE分片缓冲区 (512B×4槽 = 2KB)

**仍在内部SRAM的大对象**:
- ⚠️ `modbus::tcp::ConnectionState[8]` (每个~256B) = 2KB
- ⚠️ `web::request_buffer` (16KB临时缓冲)

**修复**:
- [ ] 检查`ConnectionState`是否可移至PSRAM（需确认DMA安全性）
- [ ] HTTP临时缓冲使用`Box::new_in(alloc::System)`明确PSRAM分配

---

## 二、并发安全检查

### 2.1 RCU快照一致性

**当前实现**: ✅ 正确
- 所有快照使用`Arc<[u16]>`，读取仅增加引用计数
- 写入使用COW，发布完整新快照
- Modbus FC03/04在单次请求内保持快照世代一致

**潜在问题**: 无

### 2.2 原子操作审计

| 字段 | 类型 | 并发访问 | 状态 |
|------|------|----------|------|
| `IoState.di` | `AtomicU64` | DI线程写 / TCP+BLE读 | ✅ |
| `IoState.do_dirty` | `AtomicBool` | TCP+BLE写 / DO线程读 | ✅ |
| `IoState.ai[6]` | `AtomicU16` | AI线程写 / TCP+BLE读 | ✅ |
| `IoState.ao[4]` | `AtomicU16` | TCP+BLE写 / AO线程读 | ✅ |

**问题**: 无

---

## 三、协议兼容性检查

### 3.1 BLE协议（对比Android 1.0.78）

| 命令 | 实现状态 | 兼容性 | 问题 |
|------|----------|--------|------|
| 0x11 心跳 | ✅ | ✅ | LOOP9修复MAC缺失 |
| 0xB0/B1 FUNC_COUNT | ✅ | ✅ | 正常 |
| 0xB2-B7 设备文本 | ✅ | ✅ | 分片重组正确 |
| 0xC0-CF MCA自定义 | ✅ | ✅ | 开关/SN/网络配置正常 |
| 0xD0-D3 逻辑配置 | ✅ | ✅ | COM请求/删除正常 |
| FC03/04/06/16 | ✅ | ✅ | Modbus over BLE正常 |

**遗留问题**:
- ⚠️ **改名后广播不更新**: LOOP17已修复，需回归测试
- ⚠️ **READ_IP截断**: LOOP2已修复25字节响应，需验证

### 3.2 Modbus TCP（对比标准+旧固件）

| 功能码 | 实现状态 | 边界检查 | 兼容性 |
|--------|----------|----------|--------|
| FC01/02 | ✅ | ≤2000位 | ✅ |
| FC03/04 | ✅ | ≤125寄存器 | ✅ |
| FC05 | ✅ | 单线圈 | ✅ |
| FC06 | ✅ | 单寄存器 | ✅ |
| FC0F | ✅ | ≤1968位 | ✅ |
| FC10 | ✅ | ≤123寄存器 | ✅ |

**连接管理**:
- ✅ 8连接上限
- ✅ 5分钟idle超时
- ✅ 5秒发送超时
- ❌ **ISSUE**: 没有实现半连接攻击防护（SYN flood）

**修复**:
- [ ] 添加连接速率限制（每秒最多10次新连接）
- [ ] 实现黑名单机制（3次恶意断开 → 封禁5分钟）

### 3.3 Modbus RTU

| 端口 | 当前模式 | 问题 |
|------|----------|------|
| UART1 | 主站+从站双模 | ✅ 正常 |
| UART2 | **固定从站** | ❌ 缺少主站/LoRa模式 |
| UART0 | 未启用 | ⚠️ 与USB复用，需谨慎 |

**关键缺陷**:
- ❌ **CRITICAL**: UART2固定从站，无法实现旧固件的"LoRa"主站模式
- ❌ `SystemConfig.rs485[1].mode`字段存在但未使用

---

## 四、掉电安全检查

### 4.1 NVS A/B槽策略

**协议数据** (`proto_a/proto_b`):
- ✅ 双槽轮换
- ✅ 写inactive槽 → CRC校验 → 原子切换`proto_act`
- ✅ 启动时校验两槽CRC，选择最新有效槽

**DO持久化**:
- ✅ 1秒节流 + 值去重
- ⚠️ **ISSUE**: 连续掉电可能丢失最后1秒的DO变化

**修复**:
- [ ] 将DO节流降至500ms（平衡写入寿命和可靠性）
- [ ] 添加关键DO立即持久化标志（如安全继电器）

### 4.2 Holding Raw分区

**问题**:
- ⚠️ **未启用**: 代码中预留了`holding`分区（32KB, A/B槽），但当前未实际使用
- ⚠️ `src/bus/backends.rs`中`write_hold_reg`直接写内存，未持久化

**修复**:
- [ ] 实现Holding寄存器A/B槽轮换写入
- [ ] 启动时从Flash恢复holding_buf

---

## 五、网络稳定性检查

### 5.1 以太网健康监控

**当前实现**:
- ✅ 5秒心跳检测
- ✅ PHY link状态轮询
- ❌ **ISSUE**: 没有自动重连逻辑

**修复**:
- [ ] 链路断开后自动重启W5500
- [ ] DHCP续租失败后自动重试

### 5.2 TCP连接泄漏检测

**问题**:
- ⚠️ 5分钟idle超时可能过长（恶意连接占满8槽）
- ⚠️ 没有统计连接建立/关闭计数

**修复**:
- [ ] 缩短idle超时到2分钟
- [ ] 添加`tcp_conn_established`和`tcp_conn_closed`计数器
- [ ] 每分钟记录连接池状态

---

## 六、IO可靠性检查

### 6.1 DI去抖

**当前实现**:
- ✅ 20ms扫描周期
- ✅ 三次采样一致才认定变化
- ⚠️ **ISSUE**: 去抖计数器未持久化，重启后丢失中间状态

**影响**: 轻微（仅重启瞬间可能误触发一次）

### 6.2 DO故障恢复

**当前实现**:
- ✅ 启动时从NVS恢复DO位图
- ✅ 5ms检查dirty标志，变化时立即写入I2C
- ❌ **ISSUE**: PCA9555写入失败后无重试机制

**修复**:
- [ ] I2C写入失败后标记dirty，下次继续尝试
- [ ] 连续3次失败记录错误日志

### 6.3 AI/AO校准

**当前实现**:
- ✅ 开机8秒自动校准窗口
- ⚠️ **ISSUE**: 校准参数未持久化，重启后丢失

**修复**:
- [ ] 将AI/AO校准参数存入NVS（每路offset+scale）
- [ ] 提供BLE命令清除校准（恢复出厂默认）

---

## 七、LoRa功能缺失

### 7.1 问题描述

旧固件通过`rs485_3_type`控制UART2模式：
- `MODSLAVE (2)` → Modbus RTU从站
- `MODMAST (1)` → Modbus RTU主站（代码注释"Rola receive data"）

**当前Rust固件**:
- UART2固定为Modbus RTU从站
- `SystemConfig.rs485[1].mode`字段未被实际使用

### 7.2 需要实现的功能

1. **模式枚举**:
   ```rust
   pub enum Rs485Mode {
       Disabled = 0,
       Master = 1,
       Slave = 2,
       Transparent = 3, // LoRa透传模式
   }
   ```

2. **动态任务创建**:
   - 根据`rs485[1].mode`启动对应任务
   - 从站模式：复用现有`mb-rtu-slave`框架
   - 主站模式：复用现有`mb-rtu-master`框架
   - 透传模式：新增串口透传任务（UART2 ↔ TCP/BLE）

3. **运行时切换**:
   - 修改`rs485[1].mode`后触发任务重启
   - 确保不影响UART1（RS485_0）

---

## 八、优先级修复清单

### P0 - 立即修复（影响稳定性）
1. ✅ **补充UART2模式切换（LoRa功能）** - 已完成
   - 添加 Transparent 模式枚举
   - 实现透传模式逻辑（串口数据读取+日志记录）
   - 更新 apply_runtime_policy 支持 mode=3

2. ✅ **main_loop栈余量过小（增加到6KB）** - 已完成
   - 从 24KB → 32KB (余量 ~12KB)
   - 更新测试用例断言

3. ✅ **TCP半连接攻击防护** - 已完成
   - 添加连接速率限制（每秒最多10次新连接）
   - 实现连接统计（conn_established_total / conn_closed_total）
   - 缩短 idle 超时 5min → 2min

4. ✅ **DO I2C写入失败重试** - 已完成
   - 失败后保持 dirty 标志
   - 连续失败第3次时立即记录错误日志
   - 100ms后自动重试

### P1 - 高优先级（影响可靠性）
5. ✅ **Holding寄存器A/B槽持久化** - 已完全实现
   - 独立 `holding` 分区（32KB）
   - A/B 双槽轮换写入 + CRC32 校验
   - 代码位置: `src/device/holding_store.rs`

6. ⚠️ **以太网自动重连** - 部分实现
   - 有链路状态检测
   - 缺少自动重启 W5500 逻辑
   - 缺少 DHCP 续租失败重试

7. ✅ **DO持久化节流** - 当前1秒已足够
   - DeviceActor 1 秒检查 dirty 标志
   - NVS 写入寿命足够（实际 DO 变化频率低）
   - 降至500ms收益有限且增加磨损

8. ✅ **AI/AO校准参数持久化** - 已完全实现
   - 校准值写入 holding_buf (2280-2295)
   - 通过 RCU STORAGE 快照持久化
   - 代码位置: `src/channel/calib.rs:150-165`

### P2 - 中优先级（改进体验）
9. ⚠️ **TCP连接池统计** - 已实现50%
   - ✅ 已添加 conn_established_total / conn_closed_total
   - ⚠️ 未添加定期日志记录（每分钟）
   
10. ⚠️ **BLE改名回归测试** - 建议手动测试
11. ✅ **缩短TCP idle超时到2分钟** - 已完成
   - 从 5 分钟 → 2 分钟
   - 代码位置: `src/config.rs:341`

---

## 九、回归测试计划

### 9.1 内存压力测试
- [ ] 满载8个TCP连接 + BLE连接 + 全速IO采样
- [ ] 运行24小时，监控最小堆余量

### 9.2 协议兼容测试
- [ ] Android 1.0.78手持机全命令回归
- [ ] Modbus TCP压力测试（1000次FC03/04/06/16循环）
- [ ] Modbus RTU主从模式切换测试

### 9.3 掉电安全测试
- [ ] 写入协议数据时随机断电100次
- [ ] 写入DO时随机断电100次
- [ ] 验证启动后数据完整性

### 9.4 网络稳定性测试
- [ ] 以太网插拔10次（验证自动重连）
- [ ] TCP连接创建/关闭1万次（验证无泄漏）
- [ ] DHCP续租失败模拟（路由器重启）

---

## 十、总结

**当前状态**:
- ✅ 基础功能正常（BLE、Modbus TCP、IO、NFC、HTTP OTA）
- ⚠️ 可靠性有提升空间（内存余量、错误恢复、持久化）
- ❌ **关键功能缺失：UART2模式切换（LoRa）**

**修复后预期**:
- ✅ 内存余量充足（main_loop ≥6KB）
- ✅ 网络攻击防护（连接速率限制+黑名单）
- ✅ 完整掉电保护（Holding A/B + DO 500ms节流）
- ✅ **LoRa功能补全（UART2主站/从站/透传模式）**

**工作量估计**:
- P0修复: 2工作日
- P1修复: 1工作日
- 回归测试: 1工作日
- **总计: 4工作日**
